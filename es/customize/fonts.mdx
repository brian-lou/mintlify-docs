---
title: "Fuentes"
description: "Personaliza la tipografía con Google Fonts o fuentes autoalojadas."
keywords: ["Google Fonts", "fuentes locales", "tipografía personalizada"]
---

Establece una fuente personalizada para todo tu sitio o configura por separado las fuentes de los encabezados y del cuerpo del texto. Usa Google Fonts, archivos de fuentes locales o fuentes alojadas externamente. La fuente predeterminada varía según el tema.

Las fuentes se configuran mediante la [propiedad `fonts`](/es/organize/settings#param-fonts) de tu `docs.json`.

<div id="google-fonts">
  ## Google Fonts
</div>

Mintlify carga automáticamente [Google Fonts](https://fonts.google.com) cuando indicas el nombre de una familia tipográfica en tu `docs.json`.

```json docs.json
"fonts": {
  "family": "Inter"
}
```

<div id="local-fonts">
  ## Fuentes locales
</div>

Para usar fuentes locales, coloca tus archivos de fuente en el directorio de tu proyecto y haz referencia a ellos en la configuración de `docs.json`.

<div id="setting-up-local-fonts">
  ### Configurar fuentes locales
</div>

<Steps>
  <Step title="Añade los archivos de fuente a tu proyecto">
    Por ejemplo, crea un directorio `fonts` y añade tus archivos de fuente:

    ```text
    your-project/
    ├── fonts/
    │   ├── InterDisplay-Regular.woff2
    │   └── InterDisplay-Bold.woff2
    ├── docs.json
    └── ...
    ```
  </Step>

  <Step title="Configura las fuentes en docs.json">
    Especifica tus fuentes locales con rutas relativas desde la raíz de tu proyecto:

    ```json docs.json
    {
      "fonts": {
        "family": "InterDisplay",
        "source": "/fonts/InterDisplay-Regular.woff2",
        "format": "woff2",
        "weight": 400
      }
    }
    ```
  </Step>
</Steps>

<div id="local-fonts-for-headings-and-body">
  ### Fuentes locales para encabezados y texto del cuerpo
</div>

Configura fuentes locales independientes para los encabezados y el texto del cuerpo en tu `docs.json`:

```json docs.json
{
  "fonts": {
    "heading": {
      "family": "InterDisplay",
      "source": "/fonts/InterDisplay-Bold.woff2",
      "format": "woff2",
      "weight": 700
    },
    "body": {
      "family": "InterDisplay",
      "source": "/fonts/InterDisplay-Regular.woff2",
      "format": "woff2",
      "weight": 400
    }
  }
}
```

<div id="externally-hosted-fonts">
  ## Fuentes alojadas externamente
</div>

Usa fuentes alojadas externamente indicando la URL de origen de la fuente en tu `docs.json`:

```json docs.json
{
  "fonts": {
    "family": "Hubot Sans",
    "source": "https://mintlify-assets.b-cdn.net/fonts/Hubot-Sans.woff2",
    "format": "woff2",
    "weight": 400
  }
}
```

<div id="font-configuration-reference">
  ## Referencia de la configuración de fuentes
</div>

<ResponseField name="fonts" type="object">
  Configuración de fuentes para tu documentación.

  <Expandable title="Fuentes">
    <ResponseField name="family" type="string" required>
      Nombre de la familia tipográfica, como &quot;Inter&quot; o &quot;Playfair Display&quot;.
    </ResponseField>

    <ResponseField name="weight" type="number">
      Peso de la fuente, como 400 o 700. Las fuentes variables admiten pesos precisos, como 550.
    </ResponseField>

    <ResponseField name="source" type="string (uri)">
      URL de origen de tu fuente, como `https://mintlify-assets.b-cdn.net/fonts/Hubot-Sans.woff2`, o ruta a tu archivo de fuente local, como `/assets/fonts/InterDisplay.woff2`. Google Fonts se carga automáticamente cuando especificas un nombre de `family` de Google Fonts, por lo que no necesitas una URL de origen.
    </ResponseField>

    <ResponseField name="format" type="'woff' | 'woff2'">
      Formato del archivo de fuente. Obligatorio cuando usas el campo `source`.
    </ResponseField>

    <ResponseField name="heading" type="object">
      Sobrescribe la configuración de la fuente específicamente para los encabezados.

      <Expandable title="Encabezado">
        <ResponseField name="family" type="string" required>
          Nombre de la familia tipográfica para los encabezados.
        </ResponseField>

        <ResponseField name="weight" type="number">
          Peso de la fuente para los encabezados.
        </ResponseField>

        <ResponseField name="source" type="string (uri)">
          URL de origen de tu fuente, como `https://mintlify-assets.b-cdn.net/fonts/Hubot-Sans.woff2`, o ruta a tu archivo de fuente local para los encabezados. Google Fonts se carga automáticamente cuando especificas un nombre de `family` de Google Fonts, por lo que no necesitas una URL de origen.
        </ResponseField>

        <ResponseField name="format" type="'woff' | 'woff2'">
          Formato del archivo de fuente para los encabezados. Obligatorio cuando usas el campo `source`.
        </ResponseField>
      </Expandable>
    </ResponseField>

    <ResponseField name="body" type="object">
      Sobrescribe la configuración de la fuente específicamente para el cuerpo del texto.

      <Expandable title="Body">
        <ResponseField name="family" type="string" required>
          Nombre de la familia tipográfica para el cuerpo del texto.
        </ResponseField>

        <ResponseField name="weight" type="number">
          Peso de la fuente para el cuerpo del texto.
        </ResponseField>

        <ResponseField name="source" type="string (uri)">
          URL de origen de tu fuente, como `https://mintlify-assets.b-cdn.net/fonts/Hubot-Sans.woff2`, o ruta a tu archivo de fuente local para el cuerpo del texto. Google Fonts se carga automáticamente cuando especificas un nombre de `family` de Google Fonts, por lo que no necesitas una URL de origen.
        </ResponseField>

        <ResponseField name="format" type="'woff' | 'woff2'">
          Formato del archivo de fuente para el cuerpo del texto. Obligatorio cuando usas el campo `source`.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>