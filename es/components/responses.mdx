---
title: "Campos de respuesta"
description: "Muestra los valores de respuesta de la API."
keywords: ["salida de API", "valores devueltos", "estructura de respuesta"]
---

El componente `<ResponseField>` está diseñado para definir los valores que devuelve una API. En muchas documentaciones, `<ResponseField>` también se usa en páginas en las que necesitas enumerar los tipos de algún elemento.

<ResponseField name="response" type="string" required>
  Un ejemplo de campo de respuesta
</ResponseField>

```mdx
<ResponseField name="response" type="string" required>
  Un ejemplo de campo de respuesta
</ResponseField>
```


<div id="props">
  ## Props
</div>

<ResponseField name="name" type="string" required>
  El nombre del valor de respuesta.
</ResponseField>

<ResponseField name="type" type="string" required>
  El tipo esperado del valor de respuesta; puede ser cualquier cadena.
</ResponseField>

<ResponseField name="default" type="string">
  El valor predeterminado.
</ResponseField>

<ResponseField name="required" type="boolean">
  Muestra &quot;required&quot; junto al nombre del campo.
</ResponseField>

<ResponseField name="deprecated" type="boolean">
  Indica si un campo está obsoleto.
</ResponseField>

<ResponseField name="pre" type="string[]">
  Etiquetas que se muestran antes del nombre del campo
</ResponseField>

<ResponseField name="post" type="string[]">
  Etiquetas que se muestran después del nombre del campo
</ResponseField>

<RequestExample>
  ```mdx Ejemplo de campo de respuesta
  <ResponseField name="response" type="string" required>
    Un ejemplo de campo de respuesta
  </ResponseField>
  ```
</RequestExample>