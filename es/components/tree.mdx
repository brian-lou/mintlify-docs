---
title: "Árbol"
description: "Usa componentes de árbol para mostrar estructuras jerárquicas de archivos y carpetas."
keywords: ["árbol", "árbol de archivos", "estructura de carpetas", "navegación jerárquica", "sistema de archivos"]
---

Usa componentes de árbol para mostrar estructuras jerárquicas, como sistemas de archivos, directorios de proyectos o contenido anidado. El componente de árbol admite navegación con teclado y funciones de accesibilidad.

<div id="basic-tree">
  ## Árbol básico
</div>

<Tree>
  <Tree.Folder name="app" defaultOpen>
    <Tree.File name="layout.tsx" />

    <Tree.File name="page.tsx" />

    <Tree.File name="global.css" />
  </Tree.Folder>

  <Tree.Folder name="lib">
    <Tree.File name="utils.ts" />

    <Tree.File name="db.ts" />
  </Tree.Folder>

  <Tree.File name="package.json" />

  <Tree.File name="tsconfig.json" />
</Tree>

```mdx Tree example
<Tree>
  <Tree.Folder name="app" defaultOpen>
    <Tree.File name="layout.tsx" />
    <Tree.File name="page.tsx" />
    <Tree.File name="global.css" />
  </Tree.Folder>
  <Tree.Folder name="lib">
    <Tree.File name="utils.ts" />
    <Tree.File name="db.ts" />
  </Tree.Folder>
  <Tree.File name="package.json" />
  <Tree.File name="tsconfig.json" />
</Tree>
```


<div id="nested-folders">
  ## Carpetas anidadas
</div>

Crea estructuras con varios niveles de anidación al anidar componentes `Tree.Folder` entre sí.

<Tree>
  <Tree.Folder name="app" defaultOpen>
    <Tree.File name="layout.tsx" />

    <Tree.File name="page.tsx" />

    <Tree.Folder name="api" defaultOpen>
      <Tree.Folder name="auth">
        <Tree.File name="route.ts" />
      </Tree.Folder>

      <Tree.File name="route.ts" />
    </Tree.Folder>

    <Tree.Folder name="components">
      <Tree.File name="button.tsx" />

      <Tree.File name="dialog.tsx" />

      <Tree.File name="tabs.tsx" />
    </Tree.Folder>
  </Tree.Folder>

  <Tree.File name="package.json" />
</Tree>

```mdx Nested folders example
<Tree>
  <Tree.Folder name="app" defaultOpen>
    <Tree.File name="layout.tsx" />
    <Tree.File name="page.tsx" />
    <Tree.Folder name="api" defaultOpen>
      <Tree.Folder name="auth">
        <Tree.File name="route.ts" />
      </Tree.Folder>
      <Tree.File name="route.ts" />
    </Tree.Folder>
    <Tree.Folder name="components">
      <Tree.File name="button.tsx" />
      <Tree.File name="dialog.tsx" />
      <Tree.File name="tabs.tsx" />
    </Tree.Folder>
  </Tree.Folder>
  <Tree.File name="package.json" />
</Tree>
```


<div id="keyboard-navigation">
  ## Navegación mediante teclado
</div>

El componente de árbol admite la navegación mediante teclado.

* **Teclas de dirección**: Navega hacia arriba y hacia abajo entre los elementos visibles.
* **Flecha derecha**: Expande una carpeta cerrada o se mueve al primer elemento hijo de una carpeta abierta.
* **Flecha izquierda**: Contrae una carpeta abierta o se mueve a la carpeta padre.
* **Home**: Salta al primer elemento del árbol.
* **End**: Salta al último elemento visible del árbol.
* **Enter/Espacio**: Alterna el estado abierto/cerrado de la carpeta.
* *****: Expande todas las carpetas del mismo nivel en el nivel actual.
* **Búsqueda al escribir**: Escribe caracteres para saltar a los elementos que empiecen con esos caracteres.

<div id="properties">
  ## Propiedades
</div>

<div id="treefolder">
  ### Tree.Folder
</div>

<ResponseField name="name" type="string" required>
  El nombre de la carpeta que se muestra en el árbol.
</ResponseField>

<ResponseField name="defaultOpen" type="boolean" default="false">
  Si la carpeta está expandida de forma predeterminada.
</ResponseField>

<ResponseField name="openable" type="boolean" default="true">
  Si la carpeta se puede abrir y cerrar. Establécelo en false para las carpetas no interactivas.
</ResponseField>

<div id="treefile">
  ### Tree.File
</div>

<ResponseField name="name" type="string" required>
  El nombre del archivo que aparece en el árbol.
</ResponseField>