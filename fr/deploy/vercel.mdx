---
title: "Vercel"
description: "Déployer la documentation dans un sous-chemin sur Vercel."
keywords: ["vercel.json", "déploiement Vercel", "configuration des réécritures", "routage par sous-chemin"]
---

import { VercelJsonGenerator } from "/snippets/fr/vercel-json-generator.mdx";

Configurez votre fichier `vercel.json` pour faire transiter, via un proxy, les requêtes de votre domaine principal vers votre documentation située sur un sous-chemin.


<div id="verceljson-file">
  ## Fichier vercel.json
</div>

Le fichier `vercel.json` configure la façon dont votre projet est construit et déployé. Il se trouve dans le répertoire racine de votre projet et contrôle différents aspects de votre déploiement, notamment le routage, les redirections, les en-têtes et les paramètres de build.

Nous utilisons la configuration `rewrites` dans votre fichier `vercel.json` pour faire transiter les requêtes de votre domaine principal vers votre documentation via un proxy.

Les `rewrites` font correspondre les requêtes entrantes à différentes destinations sans modifier l’URL dans le navigateur. Quand quelqu’un visite `yoursite.com/docs`, Vercel récupère en interne le contenu depuis `your-subdomain.mintlify.dev/docs`, mais l’utilisateur voit toujours `yoursite.com/docs` dans son navigateur. Cela diffère des redirections, qui envoient les utilisateurs vers une autre URL.

<div id="configuration">
  ## Configuration
</div>

<div id="host-at-docs-subpath">
  ### Héberger sur le sous-chemin `/docs`
</div>

1. Accédez à [Custom domain setup](https://dashboard.mintlify.com/settings/deployment/custom-domain) dans votre tableau de bord.
2. Activez l’interrupteur **Host at `/docs`**.
    <Frame>
      <img alt="Capture d’écran de la page Custom domain setup. L’interrupteur Host at `/docs` est activé et mis en évidence par un rectangle orange." src="/images/subpath/toggle-light.png" className="block dark:hidden" />
      <img alt="Capture d’écran de la page Custom domain setup. L’interrupteur Host at `/docs` est activé et mis en évidence par un rectangle orange." src="/images/subpath/toggle-dark.png" className="hidden dark:block" />
    </Frame>
3. Saisissez votre domaine.
4. Sélectionnez **Add domain**.
5. Ajoutez les réécritures suivantes à votre fichier `vercel.json`. Remplacez `[subdomain]` par votre sous-domaine, qui se trouve à la fin de l’URL de votre tableau de bord. Par exemple, `dashboard.mintlify.com/your-organization/your-subdomain` a pour identifiant de domaine `your-subdomain`.

    ```json
    {
      "rewrites": [
        {
          "source": "/docs",
          "destination": "https://[subdomain].mintlify.dev/docs"
        },
        {
          "source": "/docs/:match*",
          "destination": "https://[subdomain].mintlify.dev/docs/:match*"
        }
      ]
    }
    ```

La configuration `rewrites` mappe le sous-chemin `/docs` sur votre domaine vers le sous-chemin `/docs` de votre documentation.

- **`source`** : le modèle de chemin sur votre domaine qui déclenche la réécriture.
- **`destination`** : l’emplacement vers lequel la requête doit être proxifiée.
- **`:match*`** : un joker qui capture tous les segments de chemin après votre sous-chemin.

Pour plus d’informations, consultez [Configuring projects with vercel.json: Rewrites](https://vercel.com/docs/projects/project-configuration#rewrites) dans la documentation Vercel.

<div id="host-at-custom-subpath">
  ### Héberger sur un sous-chemin personnalisé
</div>

Pour utiliser un sous-chemin personnalisé (tout chemin autre que `/docs`), vous devez organiser vos fichiers de documentation dans votre dépôt afin qu’ils correspondent à la structure de ce sous-chemin. Par exemple, si votre documentation est hébergée à `yoursite.com/help`, vos fichiers de documentation doivent être placés dans un répertoire `help/`.

Utilisez le générateur ci-dessous pour créer votre configuration de réécritures (`rewrites`). Ajoutez ces réécritures à votre fichier `vercel.json`.

<VercelJsonGenerator />