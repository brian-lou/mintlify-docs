---
title: "Speakeasy"
description: "Automatisez les exemples de code SDK dans votre espace de test API."
keywords: ["Speakeasy SDK", "SDKs générés automatiquement", "Exemples de code SDK", "Intégration à l’espace de test API"]
---

Les extraits de code générés automatiquement à partir des SDK Speakeasy peuvent être intégrés directement dans la documentation de référence de l’API Mintlify. Les extraits d’utilisation des SDK sont affichés dans l’[espace de test interactif](/fr/api-playground/overview) des sites de documentation propulsés par Mintlify.

<Frame>
  ![Un espace de test API Mintlify avec des extraits de code Speakeasy.](/images/speakeasy/mintlify-with-speakeasy-openapi.png)
</Frame>

<div id="prerequisites">
  ## Conditions préalables
</div>

Pour intégrer Mintlify à Speakeasy, vous aurez besoin des éléments suivants :

* D’un [dépôt de documentation Mintlify](/fr/quickstart#creating-the-repository).
  * D’un SDK généré par Speakeasy avec une [URL d’exemples de code automatisés](https://www.speakeasy.com/docs/code-samples/automated-code-sample-urls) configurée.

<div id="setting-up-the-integration">
  ## Configuration de l’intégration
</div>

Pour intégrer Speakeasy à Mintlify, vous devez obtenir l’URL publique de la spécification combinée de l’API depuis le registre et mettre à jour votre fichier de configuration `docs.json`.

<div id="get-the-apis-combined-spec-public-url-from-the-registry">
  ### Obtenir l’URL publique de la spécification combinée de l’API à partir du registre
</div>

Accédez à votre [tableau de bord Speakeasy](https://app.speakeasy.com) et ouvrez l’onglet **API Registry**. Ouvrez l’entrée `*-with-code-samples` pour l’API.

<Frame>
  ![Capture d’écran de la page Speakeasy API Registry. L’onglet API Registry est mis en évidence avec un carré rouge et le numéro 1, et l’entrée pour l’API est mise en évidence avec un carré rouge et le numéro 2.](/images/speakeasy/openapi-registry-and-combined-spec.png)
</Frame>

<Note>
  Si l’entrée n’est pas étiquetée **Combined Spec**, assurez-vous que l’API dispose d’une [URL d’exemples de code générés automatiquement](https://www.speakeasy.com/docs/code-samples/automated-code-sample-urls) configurée.
</Note>

Sur la page de cette entrée dans le registre, copiez l’URL publique fournie.

<Frame>
  ![Capture d’écran montrant l’entrée du registre pour la spécification combinée, avec la fonction de copie de l’URL mise en évidence avec un carré rouge.](/images/speakeasy/copy-combined-spec-url.png)
</Frame>

<div id="update-your-docsjson-configuration-file">
  ### Mettez à jour votre fichier de configuration `docs.json`
</div>

Ajoutez l’URL de la spécification combinée dans une section **Anchors** ou **Tabs** de votre fichier `docs.json`.

Ajoutez l’URL de la spécification combinée à une ancre en mettant à jour le champ `anchor` dans votre fichier `docs.json` comme suit :

```json docs.json
{
  "anchors": [
    {
      "name": "API Reference",
      // !mark
      "openapi": "SPEAKEASY_COMBINED_SPEC_URL",
      "url": "api-reference",
      "icon": "square-terminal"
    }
  ]
}
```

Ajoutez l’URL de la spécification combinée dans un onglet en modifiant le champ `tab` du fichier `docs.json` comme suit :

```json docs.json
{
  "tabs": [
    {
      "name": "API Reference",
      "url": "api-reference",
      // !mark
      "openapi": "SPEAKEASY_COMBINED_SPEC_URL"
    }
  ]
}
```

Les extraits de code générés par Speakeasy peuvent désormais être affichés dans votre documentation d’API et manipulés dans le playground.
