---
title: "Steps"
description: "Créez des procédures étape par étape numérotées."
keywords: ["séquence", "étapes numérotées", "procédures", "étapes d'un tutoriel"]
---

import IconsOptional from "/snippets/fr/icons-optional.mdx";

Utilisez Steps pour afficher une série d’actions ou d’événements successifs. Vous pouvez ajouter autant d’étapes que nécessaire.

<Steps>
  <Step title="Première étape">
    Il s’agit d’instructions ou de contenu qui concernent uniquement la première étape.
  </Step>

  <Step title="Deuxième étape">
    Il s’agit d’instructions ou de contenu qui concernent uniquement la deuxième étape.
  </Step>

  <Step title="Troisième étape">
    Il s’agit d’instructions ou de contenu qui concernent uniquement la troisième étape.
  </Step>
</Steps>

```mdx Steps example
<Steps>
  <Step title="Première étape">
    Voici les instructions ou le contenu relatifs uniquement à la première étape.
  </Step>
  <Step title="Deuxième étape">
    Voici les instructions ou le contenu relatifs uniquement à la deuxième étape.
  </Step>
  <Step title="Troisième étape">
    Voici les instructions ou le contenu relatifs uniquement à la troisième étape.
  </Step>
</Steps>
```


<div id="steps-properties">
  ## Propriétés du composant Steps
</div>

<ResponseField name="children" type="ReactElement<StepProps>[]" required>
  Une liste de composants `Step`.
</ResponseField>

<ResponseField name="titleSize" type="string" default="p">
  La taille des titres des étapes. Valeurs possibles : `p`, `h2` et `h3`.
</ResponseField>

<div id="individual-step-properties">
  ## Propriétés de chaque étape
</div>

<ResponseField name="children" type="string | ReactNode" required>
  Le contenu d’une étape, sous forme de texte brut ou de composants.
</ResponseField>

<IconsOptional />

<ResponseField name="title" type="string">
  Le titre est le texte principal de l’étape et s’affiche à côté de l’indicateur.
</ResponseField>

<ResponseField name="stepNumber" type="number">
  Le numéro de l’étape.
</ResponseField>

<ResponseField name="titleSize" type="string" default="p">
  La taille des titres des étapes. Valeurs possibles : `p`, `h2` et `h3`.
</ResponseField>