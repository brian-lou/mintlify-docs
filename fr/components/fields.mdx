---
title: "Champs"
description: "Documentez les paramètres de requête et de réponse de l’API avec des informations sur leur type."
keywords: ["ParamField", "ResponseField", "paramètres de requête", "documentation des réponses"]
---

Utilisez les champs pour documenter les paramètres et les réponses de l’API. Il existe deux types de champs : les champs de paramètres et les champs de réponse.

<div id="parameter-field">
  ## Champ de paramètre
</div>

Le composant `<ParamField>` sert à définir des paramètres pour vos API ou SDK. L’ajout d’un `ParamField` ajoute automatiquement un [Bac à sable API](/fr/api-playground/overview).

<ParamField path="param" type="string" required>
  Un exemple de champ de paramètre
</ParamField>

```mdx
<ParamField path="param" type="string" required>
  Un exemple de champ de paramètre
</ParamField>
```

<div id="properties">
  ### Propriétés
</div>

<ParamField body="query, path, body, or header" type="string">
  Indique si le paramètre se trouve dans la query, le path, le body ou le header. Suivi du nom
  du paramètre.
</ParamField>

<ParamField body="type" type="string">
  Type attendu de la valeur du paramètre.

  Prend en charge `number`, `string`, `boolean`, `object`.

  Les tableaux peuvent être définis à l&#39;aide du suffixe `[]`. Par exemple, `string[]`.
</ParamField>

<ParamField body="required" type="boolean">
  Indique si le paramètre est obligatoire.
</ParamField>

<ParamField body="deprecated" type="boolean">
  Indique si le paramètre est obsolète.
</ParamField>

<ParamField body="default" type="any">
  Valeur par défaut renseignée lorsque la valeur de la requête est vide
</ParamField>

<ParamField body="placeholder" type="string">
  Texte d&#39;espace réservé du champ de saisie dans le playground.
</ParamField>

<ParamField body="children" type="string">
  Description du paramètre (compatible Markdown).
</ParamField>

<div id="response-field">
  ## Champ de réponse
</div>

Le composant `<ResponseField>` définit les valeurs renvoyées par une API.

<ResponseField name="response" type="string" required>
  Exemple de champ de réponse
</ResponseField>

```mdx
<ResponseField name="response" type="string" required>
  A response field example
</ResponseField>
```

<div id="properties">
  ### Propriétés
</div>

<ResponseField name="name" type="string" required>
  Le nom de la valeur de retour.
</ResponseField>

<ResponseField name="type" type="string" required>
  Le type attendu de la valeur de retour. Il peut s’agir de n’importe quelle chaîne.
</ResponseField>

<ResponseField name="default" type="string">
  La valeur par défaut.
</ResponseField>

<ResponseField name="required" type="boolean">
  Indique si la valeur de retour est requise.
</ResponseField>

<ResponseField name="deprecated" type="boolean">
  Indique si un champ est obsolète.
</ResponseField>

<ResponseField name="pre" type="string[]">
  Libellés affichés avant le nom du champ.
</ResponseField>

<ResponseField name="post" type="string[]">
  Libellés affichés après le nom du champ.
</ResponseField>