<ResponseField name="icon" type="string">
  L&#39;icône à afficher.

  Options :

  * Nom d&#39;icône [Font Awesome](https://fontawesome.com/icons), si vous avez la [propriété](/fr/organize/settings#param-icons) `icons.library` définie sur `fontawesome` dans votre `docs.json`
  * Nom d&#39;icône [Lucide](https://lucide.dev/icons), si vous avez la [propriété](/fr/organize/settings#param-icons) `icons.library` définie sur `lucide` dans votre `docs.json`
  * URL vers une icône hébergée en externe
  * Chemin vers un fichier d&#39;icône dans votre projet
  * Code SVG entouré d&#39;accolades

  Pour des icônes SVG personnalisées :

  1. Convertissez votre SVG à l&#39;aide du [convertisseur SVGR](https://react-svgr.com/playground/).
  2. Collez votre code SVG dans le champ de saisie SVG.
  3. Copiez l&#39;élément `<svg>...</svg>` complet depuis le champ de sortie JSX.
  4. Entourez le code SVG compatible JSX avec des accolades : `icon={<svg ...> ... </svg>}`.
  5. Ajustez `height` et `width` si nécessaire.
</ResponseField>

<ResponseField name="iconType" type="string">
  Le style d&#39;icône [Font Awesome](https://fontawesome.com/icons). Uniquement utilisé avec les icônes Font Awesome.

  Options : `regular`, `solid`, `light`, `thin`, `sharp-solid`, `duotone`, `brands`.
</ResponseField>