---
title: "复杂数据类型"
description: "使用 `oneOf`、`anyOf` 和 `allOf` 关键字，通过灵活的模式、可选属性和多种数据格式来描述 API。"
keywords: ["oneOf", "anyOf", "allOf", "模式组合", "多态模式"]
---

当你的 API 接受多种数据格式、包含条件字段或采用继承模式时，OpenAPI 的模式组合关键字可以帮助你为这些灵活结构编写文档。通过使用 `oneOf`、`anyOf` 和 `allOf`，你可以描述能够处理不同输入类型，或将多个模式组合为完整数据模型的 API。

<div id="oneof-anyof-allof-keywords">
  ## `oneOf`, `anyOf`, `allOf` 关键字
</div>

对于复杂数据类型，OpenAPI 提供了用于组合模式的关键字：

* `allOf`：将多个模式组合在一起 (例如合并对象或扩展基础模式) ，作用类似于 `and` 运算符。
* `anyOf`：接受匹配所提供模式中任意一个的数据，作用类似于 `or` 运算符。
* `oneOf`：接受恰好匹配所提供模式之一的数据，作用类似于 `exclusive-or` 运算符。

<Warning>Mintlify 会将 `oneOf` 和 `anyOf` 视为相同，因为在实际场景中，两者的差异很少会影响 API 的使用。</Warning>

有关这些关键字的详细规范，请参阅 [OpenAPI 文档](https://swagger.io/docs/specification/data-models/oneof-anyof-allof-not/)。

<Info>`not` 关键字目前暂不支持。</Info>

<div id="combining-schemas-with-allof">
  ### 使用 `allOf` 组合模式
</div>

当你使用 `allOf` 时，Mintlify 会对 OpenAPI 文档进行一些预处理，以便用更易读的方式展示复杂的组合关系。例如，当你用 `allOf` 组合两个对象模式时，Mintlify 会将二者的属性合并为一个对象。在利用 OpenAPI 可复用的 [组件](https://swagger.io/docs/specification/components/) 时，这一点尤其有用。

```yaml
org_with_users:
  allOf:
    - $ref: '#/components/schemas/Org'
    - type: object
      properties:
        users:
          type: array
          description: 包含组织中所有用户的数组
# ...
components:
  schemas:
    Org:
      type: object
      properties:
        id:
          type: string
          description: 组织的 ID
```

<ParamField body="org_with_users" type="object">
  <Expandable>
    <ParamField body="id" type="string">
      组织 ID
    </ParamField>

    <ParamField body="users" type="object[]">
      包含该组织所有用户的数组
    </ParamField>
  </Expandable>
</ParamField>


<div id="providing-options-with-oneof-and-anyof">
  ### 使用 `oneOf` 和 `anyOf` 提供选项
</div>

使用 `oneOf` 或 `anyOf` 时，这些选项会显示在标签页容器中。在每个子模式中指定 `title` 字段，即可为选项命名。例如，下面展示了如何显示两种不同类型的收货地址：

```yaml
delivery_address:
  oneOf:
    - title: StreetAddress
      type: object
      properties:
        address_line_1:
          type: string
          description: 收件人的街道地址
        # ...
    - title: POBox
      type: object
      properties:
        box_number:
          type: string
          description: 邮政信箱编号
        # ...
```

<ParamField body="delivery_address" type="object">
  <div className="mt-4 rounded-xl border border-gray-100 px-4 pb-4 pt-2 dark:border-white/10">
    <Tabs>
      <Tab title="街道地址">
        <ParamField body="address_line_1" type="string">
          住宅地址中的街道地址
        </ParamField>
      </Tab>

      <Tab title="邮政信箱">
        <ParamField body="box_number" type="string">
          邮政信箱编号
        </ParamField>
      </Tab>
    </Tabs>
  </div>
</ParamField>
