---
title: "徽章"
description: "使用徽章以内联或独立形式突出显示状态、标签或元数据。"
keywords: ["badge", "label", "tag", "status", "indicator"]
---

import IconsOptional from "/snippets/zh/icons-optional.mdx";

使用徽章来显示状态标识、标签或元数据。徽章既可内联在文本中使用，也可作为独立元素使用。


<div id="basic-badge">
  ## 基本徽章
</div>

<Badge>徽章</Badge>

```mdx
<Badge>Badge</Badge>
```


<div id="colors">
  ## 颜色
</div>

徽章支持多种颜色样式，用于表达不同含义。

<Badge color="gray">徽章</Badge>
<Badge color="blue">徽章</Badge>
<Badge color="green">徽章</Badge>
<Badge color="yellow">徽章</Badge>
<Badge color="orange">徽章</Badge>
<Badge color="red">徽章</Badge>
<Badge color="purple">徽章</Badge>
<Badge color="white">徽章</Badge>
<Badge color="surface">徽章</Badge>
<Badge color="white-destructive">徽章</Badge>
<Badge color="surface-destructive">徽章</Badge>

```mdx
<Badge color="gray">Badge</Badge>
<Badge color="blue">Badge</Badge>
<Badge color="green">Badge</Badge>
<Badge color="yellow">Badge</Badge>
<Badge color="orange">Badge</Badge>
<Badge color="red">Badge</Badge>
<Badge color="purple">Badge</Badge>
<Badge color="white">Badge</Badge>
<Badge color="surface">Badge</Badge>
<Badge color="white-destructive">Badge</Badge>
<Badge color="surface-destructive">Badge</Badge>
```


<div id="sizes">
  ## 尺寸
</div>

徽章提供四种尺寸，以适配你的内容层级。

<Badge size="xs">徽章</Badge>
<Badge size="sm">徽章</Badge>
<Badge size="md">徽章</Badge>
<Badge size="lg">徽章</Badge>

```mdx
<Badge size="xs">Badge</Badge>
<Badge size="sm">Badge</Badge>
<Badge size="md">Badge</Badge>
<Badge size="lg">Badge</Badge>
```


<div id="shapes">
  ## 形状
</div>

可选择圆角或胶囊形的徽章样式。

<Badge shape="rounded">徽章</Badge>
<Badge shape="pill">徽章</Badge>

```mdx
<Badge shape="rounded">Badge</Badge>
<Badge shape="pill">Badge</Badge>
```


<div id="icons">
  ## 图标
</div>

为徽章添加图标，以提供更多上下文信息。

<Badge icon="circle-check" color="green">徽章</Badge>
<Badge icon="clock" color="orange">徽章</Badge>
<Badge icon="ban" color="red">徽章</Badge>

```mdx
<Badge icon="circle-check" color="green">Badge</Badge>
<Badge icon="clock" color="orange">Badge</Badge>
<Badge icon="ban" color="red">Badge</Badge>
```


<div id="stroke-variant">
  ## 描边变体
</div>

使用描边变体可呈现更低调的视觉效果。

<Badge stroke color="blue">徽章</Badge>
<Badge stroke color="green">徽章</Badge>
<Badge stroke color="orange">徽章</Badge>
<Badge stroke color="red">徽章</Badge>

```mdx
<Badge stroke color="blue">Badge</Badge>
<Badge stroke color="green">Badge</Badge>
<Badge stroke color="orange">Badge</Badge>
<Badge stroke color="red">Badge</Badge>
```


<div id="disabled-state">
  ## 禁用状态
</div>

将徽章设为禁用状态，以表示其处于未激活或不可用状态。

<Badge disabled icon="lock" color="gray">徽章</Badge>
<Badge disabled icon="lock" color="blue">徽章</Badge>

```mdx
<Badge disabled icon="lock" color="gray">Badge</Badge>
<Badge disabled icon="lock" color="blue">Badge</Badge>
```


<div id="inline-usage">
  ## 内联用法
</div>

徽章可自然融入文本内容中。例如，此功能需要 <Badge color="orange" size="sm">高级版</Badge> 订阅，或者此 API 端点返回 <Badge color="green" size="sm">JSON</Badge> 格式。

```mdx
This feature requires a <Badge color="orange" size="sm">Premium</Badge> subscription.
```


<div id="combined-properties">
  ## 组合属性
</div>

组合多个属性，以创建自定义徽章样式。

<Badge icon="star" color="blue" size="lg" shape="pill">高级版</Badge>
<Badge icon="check" stroke color="green" size="sm">已验证</Badge>
<Badge icon="badge-alert" color="orange" shape="rounded">测试版</Badge>

```mdx
<Badge icon="star" color="blue" size="lg" shape="pill">Premium</Badge>
<Badge icon="check" stroke color="green" size="sm">Verified</Badge>
<Badge icon="badge-alert" color="orange" shape="rounded">Beta</Badge>
```


<div id="properties">
  ## 属性
</div>

<ResponseField name="color" type="string" default="gray">
  徽章颜色样式。

  可选值：`gray`、`blue`、`green`、`yellow`、`orange`、`red`、
  `purple`、`white`、`surface`、`white-destructive`、`surface-destructive`。
</ResponseField>

<ResponseField name="size" type="string" default="md">
  徽章尺寸。

  可选值：`xs`、`sm`、`md`、`lg`。
</ResponseField>

<ResponseField name="shape" type="string" default="rounded">
  徽章形状。

  可选值：`rounded`、`pill`。
</ResponseField>

<IconsOptional />

<ResponseField name="stroke" type="boolean" default="false">
  以描边样式显示徽章，而不是使用填充背景。
</ResponseField>

<ResponseField name="disabled" type="boolean" default="false">
  以禁用状态显示徽章，并降低其不透明度。
</ResponseField>

<ResponseField name="className" type="string">
  要应用于徽章的附加 CSS 类。
</ResponseField>