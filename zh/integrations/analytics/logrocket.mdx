---
title: "LogRocket"
description: "使用 LogRocket 监控用户会话和错误。"
keywords: ["LogRocket 集成", "会话回放", "错误监控", "应用 ID"]
---

在 `docs.json` 文件中添加以下内容，以将分析数据发送至 LogRocket。

```json Analytics options in docs.json
"integrations": {
    "logrocket": {
        "apiKey": "required"
    }
}
```
