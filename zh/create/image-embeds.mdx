---
title: "图片和嵌入内容"
description: "添加图片、视频和 iframe。"
keywords: ["图片", "视频", "iframe", "media", "svg"]
---

在文档中添加图片、嵌入视频，并通过 iframe 插入交互式内容。

<Frame>
  <img className="rounded-xl" src="https://mintlify-assets.b-cdn.net/bigbend.jpg" alt="一张风景照片：前景是紫色花朵，背景是群山，上方是点缀着零散云朵的蓝天。" />
</Frame>

<div id="images">
  ## 图片
</div>

添加图片，为文档提供视觉说明、示例或装饰效果。

<div id="basic-image-syntax">
  ### 基本图片语法
</div>

使用 [Markdown 语法](https://www.markdownguide.org/basic-syntax/#images)在文档中添加图片：

```mdx
![Alt text describing the image](/path/to/image.png)
```

<Tip>
  始终添加描述性的 alt 文本，以提升无障碍访问体验和 SEO 效果。alt 文本应清楚说明图片展示的内容。
</Tip>

图片文件必须小于 20 MB。对于更大的文件，请将其托管到 [Amazon S3](https://aws.amazon.com/s3) 或 [Cloudinary](https://cloudinary.com) 等 CDN 服务。

<div id="html-image-embeds">
  ### HTML 图片嵌入
</div>

如需更灵活地控制图片显示方式，请使用 HTML `<img>` 标签：

```jsx
<img 
  src="/images/dashboard.png" 
  alt="Main dashboard interface"
  style={{height: "300px", width: "400px"}}
  className="rounded-lg"
/>
```

<div id="resize-images-with-inline-styles">
  #### 使用内联样式调整图像大小
</div>

使用带 `style` 属性的 JSX 内联样式来调整图像大小：

```jsx
<img
  src="/images/architecture.png"
  style={{width: "450px", height: "auto"}}
  alt="Diagram showing the architecture of the system"
/>
```

<div id="disable-image-zoom">
  #### 禁用图片缩放
</div>

若要禁用图片默认的点击缩放效果，请添加 `noZoom` 属性：

```html highlight="4"
<img 
  src="/images/screenshot.png" 
  alt="Descriptive alt text"
  noZoom
/>
```

<div id="link-images">
  #### 图片链接
</div>

要让图片变成可点击的链接，请将图片包裹在锚标签中，并添加 `noZoom` 属性：

```html
<a href="https://mintlify.com" target="_blank">
  <img 
    src="/images/logo.png" 
    alt="Mintlify logo"
    noZoom
  />
</a>
```

<Note>
  锚点标签内的图片会自动显示指针光标，表示该图片可点击。
</Note>

<div id="light-and-dark-mode-images">
  #### 浅色与深色模式下的图片
</div>

如需为浅色和深色主题显示不同的图片，请使用 Tailwind CSS 类：

```html
<!-- 浅色模式图片 -->
<img 
  className="block dark:hidden" 
  src="/images/light-mode.png" 
  alt="浅色模式界面"
/>

<!-- 深色模式图片 -->
<img 
  className="hidden dark:block" 
  src="/images/dark-mode.png" 
  alt="深色模式界面"
/>
```

<div id="svg-images">
  ### SVG 图像
</div>

使用 `foreignObject` 元素的 SVG 文件在生产环境中的渲染效果与本地开发环境不同。出于安全考虑，Mintlify 的图片 CDN 会从 SVG 中移除 `foreignObject`，这可能导致文本和其他嵌入的 HTML 内容被截断或隐藏。

这类问题常见于从 [draw.io](https://www.drawio.com) 等工具导出的 SVG，尤其是在启用了 HTML 文本格式或自动换行时。要解决此问题，请在导出为 SVG 之前，先为图表中的所有标签禁用 **Formatted Text** 和 **Word Wrap**。有关 SVG 导出的更多信息，请参阅 [draw.io 文档](https://www.drawio.com/doc/faq/svg-export-text-problems)。

<div id="videos">
  ## 视频
</div>

Mintlify 支持在 Markdown 中使用 [HTML 标签](https://www.markdownguide.org/basic-syntax/#html)，方便你灵活创建更丰富的内容。

<Tip>
  请务必在 video 元素中提供后备文本，以便不支持视频播放的浏览器也能显示相应内容。
</Tip>

<div id="youtube-embeds">
  ### 嵌入 YouTube 视频
</div>

使用 iframe 元素嵌入 YouTube 视频：

```html
<iframe
  className="w-full aspect-video rounded-xl"
  src="https://www.youtube.com/embed/4KzFe50RQkQ"
  title="YouTube video player"
  allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
  allowFullScreen
></iframe>
```

<Frame>
  <iframe className="w-full aspect-video rounded-xl" src="https://www.youtube.com/embed/4KzFe50RQkQ" title="YouTube 视频播放器" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowFullScreen />
</Frame>

<div id="self-hosted-videos">
  ### 自托管视频
</div>

对于自托管视频，请使用 HTML `<video>` 元素：

```html
<video
  controls
  className="w-full aspect-video rounded-xl"
  src="link-to-your-video.com"
></video>
```

<div id="autoplay-videos">
  ### 自动播放视频
</div>

如需让视频自动播放，请使用：

```html
<video
  autoPlay
  muted
  loop
  playsInline
  className="w-full aspect-video rounded-xl"
  src="/videos/demo.mp4"
></video>
```

<Note>
  使用 JSX 语法时，由两个单词组成的属性请采用驼峰命名：`autoPlay`、`playsInline`、`allowFullScreen`。
</Note>

<div id="iframes">
  ## iframes
</div>

使用 iframe 元素嵌入外部内容：

```html
<iframe 
  src="https://example.com/embed" 
  title="Embedded content"
  className="w-full h-96 rounded-xl"
></iframe>
```

<div id="related-resources">
  ## 相关资源
</div>

<Card title="Frame 组件参考" icon="frame" horizontal href="/zh/components/frames">
  了解如何使用 Frame 组件展示图像。
</Card>