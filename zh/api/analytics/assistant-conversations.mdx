---
openapi: /zh/analytics.openapi.json GET /api/external/v1/analytics/{projectId}/assistant
keywords: [ "分析", "助手", "会话", "导出", "AI" ]
---

<div id="usage">
  ## 使用方法
</div>

使用此接口从文档中导出 AI 助手的对话历史。每个对话包含用户查询、助手回复、引用的来源以及查询类别。

使用响应中返回的 `cursor` 参数对结果进行分页；当 `hasMore` 为 `true` 时继续获取。

<div id="filtering">
  ## 过滤
</div>

使用 `dateFrom` 和 `dateTo` 参数按日期范围筛选对话。

<div id="conversation-data">
  ## 会话数据
</div>

每个会话包括：

- **query**: 用户的问题
- **response**: 助手的回答
- **sources**: 回答中引用的页面，其标题和 URL
- **queryCategory**: 查询类型分类（如有）