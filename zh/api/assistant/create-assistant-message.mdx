---
openapi: /zh/discovery-openapi.json POST /v1/assistant/{domain}/message
tag: "已弃用"
keywords: [ "助手消息", "嵌入", "聊天", "集成", "AI SDK v4" ]
---

<Badge color="orange">已弃用</Badge>

<Info>
  助手消息 v1 接口与 **AI SDK v4** 兼容。如果你使用的是 AI SDK v5 或更高版本，请改用 [助手消息 v2 接口](/zh/api-reference/assistant/create-assistant-message-v2)。
</Info>

<div id="integration-with-usechat">
  ## 与 `useChat` 集成
</div>

Vercel 的 AI SDK 中提供的 `useChat` Hook 是将 Assistant API 集成到你的应用中的推荐方式。

<Steps>
  <Step title="安装 AI SDK v4">
    ```bash
    npm i ai@^4.1.15
    ```
  </Step>

  <Step title="使用 useChat Hook">
    ```tsx
    import { useChat } from 'ai/react';

    function MyComponent({ domain }) {
      const { messages, input, handleInputChange, handleSubmit, isLoading } = useChat({
        api: `https://api.mintlify.com/discovery/v1/assistant/${domain}/message`,
        headers: {
          'Authorization': `Bearer ${process.env.PUBLIC_MINTLIFY_ASSISTANT_KEY}`,
        },
        body: {
          fp: 'anonymous',
          retrievalPageSize: 5,
          context: [
            {
              type: 'code',
              value: 'const example = "code snippet";',
              elementId: 'code-block-1',
            },
          ],
        },
        streamProtocol: 'data',
        sendExtraMessageFields: true,
      });

      return (
        <div>
          {messages.map((message) => (
            <div key={message.id}>
              {message.role === 'user' ? 'User: ' : 'Assistant: '}
              {message.content}
            </div>
          ))}
          <form onSubmit={handleSubmit}>
            <input value={input} onChange={handleInputChange} />
            <button type="submit">Send</button>
          </form>
        </div>
      );
    }
    ```

    **Mintlify 必需的配置：**

    * `streamProtocol: 'data'` - 启用流式响应所必需。
    * `sendExtraMessageFields: true` - 发送消息元数据所必需。
    * `body.fp` - 指纹标识符（使用 `'anonymous'` 或某个用户标识符）。
    * `body.retrievalPageSize` - 要使用的搜索结果数量（推荐：5）。

    **可选配置：**

    * `body.context` - 提供给助手的上下文信息数组。每个 context 对象包含：
      * `type` - `'code'` 或 `'textSelection'` 之一。
      * `value` - 代码片段或选中的文本内容。
      * `elementId`（可选）- 包含该上下文的 UI 元素标识符。
  </Step>
</Steps>

更多详情请参阅 AI SDK 文档中的 [useChat](https://ai-sdk.dev/docs/reference/ai-sdk-ui/use-chat)。

<div id="rate-limits">
  ## 调用频率限制
</div>

assistant API 具有以下限制：

* 每个 API 密钥每月最多使用 10,000 次
* 每个 Mintlify 组织每小时最多 10,000 个请求
* 每个 IP 每天最多 10,000 个请求